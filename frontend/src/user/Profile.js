import React from "react";

// Components
import Base from "../core/Base";

const Profile = () => {
  return (
    <Base title="Profile Page">
      <h1>Profile Page</h1>
    </Base>
  );
};

export default Profile;
